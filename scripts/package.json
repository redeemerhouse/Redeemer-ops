{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "performance:audit": "tsx src/performance-audit.ts",
    "hello": "tsx ./src/hello.ts",
    "db:baseline": "tsx ./src/db-baseline.ts",
    "db:post-merge": "tsx ./src/db-post-merge.ts",
    "db:integrity-preflight": "tsx ./src/db-integrity-preflight.ts",
    "db:release-check": "tsx ./src/db-release-check.ts",
    "test:db-baseline-integration": "bash ./test/db-baseline-integration.sh",
    "test:db-release-check": "node --import tsx --test src/db-release-check.integration.test.ts",
    "test:db-schema-drift": "node --import tsx --test src/db-schema-drift.test.ts",
    "test:db-integrity-preflight": "node --import tsx --test src/db-integrity-preflight.test.ts",
    "test:db-tls-policy": "node --import tsx --test src/db-tls-policy.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },

  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "^8.20.0",
    "tsx": "catalog:"
  }
}
